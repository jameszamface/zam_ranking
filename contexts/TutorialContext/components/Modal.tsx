import React, {useEffect} from 'react';
import {View} from 'react-native';
import {Modal as ModaType} from '../types/Action';

function Modal(props: ModaType) {
  return <View />;
}

export default Modal;
